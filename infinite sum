function sumSeries(start, end, step) {
  let sum = 0;
  for (let i = start; i <= end; i += step) {
    sum += i;
  }
  return sum;
}

const seriesSum = sumSeries(1, 10, 2); // Series: 1 + 3 + 5 + ... + 9
console.log("Series sum:", seriesSum); // Output: 25
